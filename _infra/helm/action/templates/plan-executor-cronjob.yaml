apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: {{ .Values.crons.planJobExecutor.name }}
spec:
  schedule: "{{ .Values.crons.planJobExecutor.cron }}"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: {{ .Values.crons.planJobExecutor.name }}
              image: radial/busyboxplus:curl
              env:
                - name: SECURITY_USER_NAME
                  valueFrom:
                    secretKeyRef:
                      name: security-credentials
                      key: security-user
                - name: SECURITY_USER_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: security-credentials
                      key: security-password
                - name: TARGET
                  value: {{ .Values.crons.planJobExecutor.target }}
              args:
                - /bin/sh
                - -c
                - curl -u $(SECURITY_USER_NAME):$(SECURITY_USER_PASSWORD) http://$(ACTION_SERVICE_HOST):$(ACTION_SERVICE_PORT)/$(TARGET)
          restartPolicy: OnFailure
